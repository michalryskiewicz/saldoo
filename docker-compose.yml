services:
  # No database service: the only persistent state is a cache of public NBP rates,
  # which lives in a SQLite file on the volume below. Losing it costs a few
  # requests to NBP, nothing more.
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    restart: unless-stopped
    env_file: .env
    volumes:
      - 'rates_cache:/app/apps/backend/prisma/data'
    command: sh -c "bunx prisma migrate deploy && bun run index.ts"

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
      args:
        - VITE_SERVER_URL=${VITE_SERVER_URL}
        - VITE_GOOGLE_CLIENT=${VITE_GOOGLE_CLIENT}
        - VITE_GA_DRIVE_DIRECTORY=${VITE_GA_DRIVE_DIRECTORY}
        - VITE_GA_DRIVE_FILE=${VITE_GA_DRIVE_FILE}
        - VITE_ASSETS_DIR=${VITE_ASSETS_DIR}
        - VITE_UMAMI_WEBSITE_ID=${VITE_UMAMI_WEBSITE_ID}
        - VITE_UMAMI_SRC=${VITE_UMAMI_SRC}
    restart: unless-stopped
    depends_on:
      - backend

volumes:
  rates_cache: null
